<?xml version="1.0" encoding="utf-8"?>
<!--
  Stub config for F2. Broad event types now; F4 narrows these to the events the
  detection engine actually consumes (TYPE_VIEW_SCROLLED, TYPE_WINDOW_STATE_CHANGED)
  and scopes packageNames to monitored apps.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeViewScrolled"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
